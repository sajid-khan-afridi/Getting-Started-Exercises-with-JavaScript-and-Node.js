var items = ["bread", "eggs", "mayanise"];
function sandwich(params) {
  console.log("Sandwich is composed of ");
  for (i = 0; i <= sandwich.length + 1; i++) console.log(params[i]);
}
sandwich(items);
